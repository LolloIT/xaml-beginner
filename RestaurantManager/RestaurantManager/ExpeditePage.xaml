<Page
    x:Class="RestaurantManager.ExpeditePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RestaurantManager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.10*"/>
            <RowDefinition Height="0.80*"/>
            <RowDefinition Height="0.10*"/>
        </Grid.RowDefinitions>
        <GridView Grid.Row="0" />
        <AppBar ClosedDisplayMode="Minimal">
            <AppBarButton x:Name="appBarButton_Home" Grid.Row="0" Icon="Home" Label="Home" Click="appBarButton_Home_Click" />
        </AppBar>
        <GridView x:Name="gridOrders" Grid.Row="1"></GridView>
        
        <Button x:Name="btnSubmit" Content="Clear All Orders" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="2" Click="btnSubmit_Click" />
    </Grid>
      

</Page>
